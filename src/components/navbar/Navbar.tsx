import { ModeToggle } from "../mode-toggle";

const Navbar = () => {
  return (
    <header className=" shadow-md w-full container py-2">
      <ModeToggle />
    </header>
  );
};

export default Navbar;
